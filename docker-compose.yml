name: kennemer
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: kennemer:prod
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:80/healthz').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s
    environment:
      NODE_ENV: production
      PORT: "80"
      HOST: "0.0.0.0"
      HTTPS_PORT: "443" # optional: remove or change if you don't need TLS
      HTTPS_KEY_PATH: /certs/server.key
      HTTPS_CERT_PATH: /certs/server.crt
      # HTTPS_CA_PATH: /certs/ca.pem
      # HTTPS_PASSPHRASE: example-passphrase
    ports:
      - "80:80"
      - "443:443"
    working_dir: /app
    env_file:
      - ./app/.env
    # volumes: # uncomment to mount your own TLS certs over the baked-in self-signed ones
    #   - ./certs/server.key:/certs/server.key:ro
    #   - ./certs/server.crt:/certs/server.crt:ro
    #   - ./certs/ca.pem:/certs/ca.pem:ro
